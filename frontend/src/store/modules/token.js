const state = {
  token: null
}

const getters = {
  getToken: state => {
    return state.token
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
