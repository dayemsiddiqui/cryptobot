const state = {
  isUserLoggedIn: false
}

const getters = {
  isUserLoggedIn: state => {
    return state.isUserLoggedIn
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
