const state = {
  user: null
}

const getters = {
  getUser: state => {
    return state.user
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
